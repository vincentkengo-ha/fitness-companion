implement CreateTask
remove unnecessary code
add new countable in CreateTask
make the Run screen

Porblems:
    post run screen:
        display tempo 
        save distance
        time
        ambition:
            show how tempo and step frequency changes with time
    fix ts errors

    runs[runs.length].tempo,
                  runs[runs.length - 1].tempo,
                  runs[runs.length - 2].tempo,
                  runs[runs.length - 3].tempo,
                  runs[runs.length - 4].tempo,
                  runs[runs.length - 5].tempo,